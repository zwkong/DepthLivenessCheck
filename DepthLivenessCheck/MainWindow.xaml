<Window x:Class="DepthLivenessCheck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DepthLivenessCheck"
        mc:Ignorable="d"
        Closing="WindowClosing"
        Title="Depth Liveness" Height="500" Width="1000">
    <Grid>
        <!--<TextBox HorizontalAlignment="Right"
                 VerticalAlignment="Center"
                 TextAlignment="Center"
                 Height="360"
                 Width="480"
                 Margin="20"
                 IsReadOnly="True"
                 FontFamily="Consolas"
                 Text="{Binding DepthInfo}"/>-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtTimeStamp" Grid.Column="0" />

        <Image x:Name="imgDepth" Grid.Column="0" />
        <Image x:Name="imgColor" Grid.Column="1" />
    </Grid>
</Window>
